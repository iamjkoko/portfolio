/** Window events used to coordinate across component trees. */

/** Fired by Home when the intro loading sequence finishes (or was already seen). */
export const INTRO_COMPLETE_EVENT = 'introComplete';

/** Fired by App when a page exit animation completes, so scroll can reset. */
export const ROUTE_EXIT_COMPLETE_EVENT = 'route-exit-complete';
